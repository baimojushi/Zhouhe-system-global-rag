services:
  rag-gui:
    build:
      context: .
      dockerfile: Dockerfile.local
    container_name: global-rag-gui
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:3000"
    init: true
